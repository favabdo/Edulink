# Eduspace — ذاكرة المشروع وقواعده

> يُقرأ هذا الملف تلقائيًا في كل جلسة عمل داخل هذا المجلد. الغرض منه أن أي جلسة جديدة تعرف فورًا **أين توقف العمل** و**ما هي القرارات والتقنيات المتفق عليها**، بدون إعادة السؤال من الصفر.
>
> **اقرأ `docs/04-progress-log.md` أولًا** لمعرفة آخر نقطة توقف.

---

## قواعد إلزامية

1. **الرد باللغة العربية دائمًا**، مع إبقاء المصطلحات التقنية والمسميات (زي `LiveKit`, `SQL Server`, أسماء الأدوار) كما هي.
2. **ممنوع كتابة أي كود أو إنشاء أي ملف تنفيذي قبل أن يقول صاحب المشروع صراحةً: «ابدأ».**
3. مجلد `docs/` هو **المصدر الوحيد للحقيقة**. أي قرار جديد يُسجَّل فيه فورًا، و`docs/04-progress-log.md` يُحدَّث في نهاية كل خطوة عمل.

### قواعد قاعدة البيانات (إلزامية — طلب صاحب المشروع)

4. **ملف واحد لكل تغييرات البيانات:** `database/schema.sql` هو **الملف الوحيد** للجداول والأعمدة والفهارس والقيود. أي إضافة أو تعديل يُضاف إلى **نفس الملف** في قسم «إضافات جديدة»، بالترتيب في الأسفل. **لا تُنشأ ملفات migrations منفصلة مهما كبر الملف.**
5. **الملف تراكمي وقابل لإعادة التشغيل:** لا يُحذف منه شيء، وكل عبارة جديدة تُكتب محميّة بـ `IF NOT EXISTS` / `DROP ... IF EXISTS` فيُشغَّل مرة واحدة أو عشر مرات بنفس النتيجة.
6. **اسم كل جدول جديد يبدأ بـ `Edu_`** ثم اسم الجدول: `Edu_Users`, `Edu_Sessions`, `Edu_Notifications` ...
7. **التوافق بين المحرّكين:** الكود يعمل الآن على **Supabase (PostgreSQL)**، ومستقبلاً على **SQL Server** تابع للشركة. لذلك:
   - قسم PostgreSQL هو المُنفَّذ، و**قسم SQL Server جاهز مُعلَّق في آخر نفس الملف**.
   - **لا تستخدم ميزة خاصة بمحرّك واحد** (زي `pgcrypto` أو أنواع `jsonb`) إلا ومعها بديلها مكتوب في قسم SQL Server.
   - **التجزئة (Hashing) تتم في طبقة التطبيق** لا في القاعدة — لأن SQL Server لا يملك دوال تجزئة مكافئة.
8. **الأسماء بحالة أحرف محفوظة** (`"Edu_Users"` بين تنصيص) لتطابق SQL Server، فاكتب الأسماء **بين علامتي تنصيص دائمًا** في استعلامات PostgreSQL.
9. التفاصيل الكاملة وخريطة الأدوار في `docs/07-database.md`، ودليل التشغيل في `eduspace-web/db/README.md`.

---

## هوية المشروع

- **الاسم النهائي: `EduLink`**
- الشعار النصي: `Learn • Teach • Grow`
- المنتج: منصة تعليمية أونلاين **All-in-One** تُبنى مرة واحدة وتُباع لأكثر من مؤسسة (**White-label**).
- نموذج التسليم: لكل مؤسسة **نسخة منفصلة** — دومين خاص + قاعدة بيانات مستقلة، مع تغيير الاسم واللوجو والهوية فقط. **نفس كود الـ backend** يخدم كل المؤسسات، ويُحدَّد العميل عن طريق الدومين.
- المالك / العميل المستهدف: مؤسسات تشغّل مدرسين وتجلب طلابًا.

---

## الحالة الحالية (لحظة كتابة هذا الملف)

- **آخر تحديث:** 2026-09-11
- **المرحلة:** صفحة اللوجين مكتملة ومتجاوبة، **والباك اند يعمل** بمصادقة كاملة، **وتسجيل الدخول موصول** والصفحة الداخلية (اللوحة) مبنية.
- **قاعدة البيانات:** `database/schema.sql` — ملف واحد تراكمي فيه 4 جداول (`Edu_Users`, `Edu_Students`, `Edu_Parents`, `Edu_Relations`). **يعمل على Supabase الآن، ونسخة SQL Server مُعلَّقة في آخره.**
- **الباك اند:** `apps/api` — ASP.NET Core (.NET 10) + EF Core. دليل التشغيل ونقاط النهاية في `apps/api/README.md`، والتصميم في `docs/08-backend.md`.
- **الكود (الواجهة):** `apps/web/` (Vite + React + TypeScript) — `npm run build` و `npm run lint` نظيفان.

### كيف تشغّل المشروع كاملًا (تطوير)

```bash
# من جذر المستودع
npm install                                   # تثبيت كل الـ workspaces مرة واحدة
npm run dev                                   # الواجهة → http://localhost:5173

# الباك اند في نافذة أخرى
cd apps/api && dotnet run --launch-profile http   # → http://localhost:5279
```

**إنشاء أول حساب** (يعمل مرة واحدة فقط عند قاعدة فارغة):
```bash
curl -X POST http://localhost:5279/api/setup/owner -H "Content-Type: application/json" \
  -d '{"name":"مالك المنصة","email":"owner@edulink.test","password":"Owner@12345"}'
```

⚠️ **لا تنشئ جداول عبر EF Migrations أبدًا** — المخطط من `database/schema.sql` فقط.

### الاستراكشر — قواعد لا تُخالَف

المشروع **monorepo**: `apps/web` (واجهة) · `apps/api` (باك اند) · `packages/shared-types` · `database/` · `docs/`.
**التفاصيل الكاملة في `docs/09-structure.md`.** وأهم القواعد:

1. **صفحة = مجلد + مسار:** كل صفحة في `apps/web/src/pages/<Name>Page/` ولها مسار في `app/router/paths.ts` وسطر في `app/router/AppRouter.tsx`.
2. **ممنوع كتابة مسار نصًّا** في أي مكوّن — استخدم `ROUTES.*`.
3. **`features/` للمنطق و`pages/` للعرض** — النداءات والأنواع والسياق في `features/`، والصفحة تجمع وتُعرض.
4. **الترجمة namespace لكل صفحة:** ملف JSON في `apps/web/src/locales/<lang>/` + استخدام `t('login:form.submit')`.
5. **الباك اند (المستهدف):** Clean Architecture بأربع طبقات `Domain ← Application ← Infrastructure ← API` (نفس معيار مشروع `NileTechno`). لم يُنفَّذ بعد.
- **النشر:** الإعدادات في `eduspace-web/vercel.json`، والخطوط مستضافة محليًا في `public/fonts/`. الدليل الكامل في `docs/06-deployment.md`.
- **المستودع:** `https://github.com/favabdo/Edulink` — الفرع `main`، و**جذر المستودع هو مجلد `eduspace-web` نفسه** (بلا Root Directory في Vercel). ملاحظة: مجلد `docs/` **خارج** المستودع.
- **التجاوب:** المقياس `--u` **ثابت `1px`** (لا يعتمد على أبعاد الشاشة، حتى يعمل الزووم طبيعيًا) والتجاوب عبر media queries. القاعدة عند إضافة عنصر: استخدم `calc(N * var(--u))`، وتأكد أن الكارت + الحشو يدخل في 768px. التفاصيل في `docs/05-project-structure.md`.
- **التثبيت على الموبايل:** `public/manifest.webmanifest` + أيقونات في `public/icons/`.
- **السيرفر:** `npm run dev` يعمل على المنفذ **5174**.
- **الخطوة التالية المقترحة:** مخطط قاعدة بيانات SQL Server الكامل (ERD)، أو لوحة تحكم المشرف — بانتظار قرار صاحب المشروع.
- **التفاصيل الكاملة دائمًا في `docs/04-progress-log.md`.**

---

## فهرس الوثائق

| الملف | المحتوى |
|---|---|
| `docs/01-requirements.md` | كل المتطلبات والقرارات الوظيفية (الأدوار، الحجز، الحصة، اللغات، التقارير، المستثنى) |
| `docs/02-tech-stack.md` | الستاك التقني والتوصيات وأسبابها، وملاحظات الترخيص والسكيل |
| `docs/03-design-reference.md` | وصف الريفيرنس التصميمي (صفحة لوجين EduLink) والقياسات والألوان |
| `docs/04-progress-log.md` | **سجل التقدم خطوة بخطوة + آخر نقطة توقف** |
| `docs/05-project-structure.md` | بنية الكود والتقنيات المستخدمة فعلًا وقراراتها |
| `docs/06-deployment.md` | النشر على Vercel + الأداء |
| `docs/07-database.md` | قواعد قاعدة البيانات + الجداول + خطة SQL Server |
| `docs/08-backend.md` | الباك اند: التقنية، البنية، تدفق الدخول، الصفحة الداخلية |
| `docs/09-structure.md` | **استراكشر المشروع وقواعده (اقرأه قبل إضافة أي صفحة)** |
| `docs/10-supabase-connection.md` | **ربط Supabase بالمشروع + كيف تتأكد أن القراءة منها** |
| `database/schema.sql` | **الملف الوحيد لكل تغييرات قاعدة البيانات** |
| `apps/api/README.md` | تشغيل الباك اند + نقاط النهاية |
| `docs/reference/` | صور الريفيرنس الأصلية |
| `docs/screenshots/` | لقطات التنفيذ الفعلي (عربي/إنجليزي × فاتح/غامق) |
| `docs/history/` | النصوص الخام للجلسات السابقة (مرجع تاريخي) |

---

## ملخص القرارات الحرجة (التفاصيل في `docs/01-requirements.md`)

- **قاعدة البيانات:** `SQL Server`.
- **نموذج البيع:** White-label — نسخة ودومين وقاعدة بيانات لكل مؤسسة.
- **الفلوس:** **خارج المنصة تمامًا.** مفيش محافظ ولا كوبونات ولا فواتير ولا VAT ولا بوابات دفع. المنصة تحسب بس **عدد الساعات لكل مدرس** + إحصائيات الحضور والالتزام.
- **الحصة:** 1 مدرس + 1 طالب دايمًا، وممكن مشرف/إدارة يدخل **مراقبًا (observer)**.
- **الفيديو:** يُبنى عندنا بتقنية تتوسّع أفقيًا — التوصية **`LiveKit`** + سبورة **`Excalidraw`**.
- **التسجيل (Recording):** **ممنوع** — يُحفظ الـ metadata فقط (المدة، وقت البداية والنهاية، الحضور).
- **التسجيل العام (Sign-up):** مقفول — الإدارة/المشرف يضيف كل المستخدمين يدويًا، والدخول بإيميل Google أو رقم تليفون.
- **اللغات:** عربي + إنجليزي **معًا من أول يوم** (RTL/LTR بزر تبديل).
- **الثيم:** فاتح + غامق فقط (بدون تحكم من لوحة التحكم).
- **الأجهزة:** الدخول من أكثر من جهاز مسموح.

---

## الأسماء والمسميات المعتمدة (استخدمها كما هي في الكود)

- الأدوار بالعربي: **مالك — مسئول — مشرف — طالب — ولي أمر**
- المقابل بالإنجليزي (مقترح للكود): `owner` — `admin` — `supervisor` — `student` — `parent`
- نوع الجلسة: 1-to-1 Live Session
- الأدوار الإضافية في الحصة: `observer` (إدارة/مشرف يراقب فقط)
